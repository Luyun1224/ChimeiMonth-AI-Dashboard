// Lightweight loader: defer to the readable script during development so charts use the latest logic.
(function(){
    try{
        var s=document.createElement('script');
        s.src='script.js';
        s.defer=true;
        document.head.appendChild(s);
    }catch(e){
        console.error('Failed to load script.js fallback loader',e);
    }
})();
